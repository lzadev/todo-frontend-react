const Title = ({ title }) => {
  return (
    <div className="mt-5 animate__animated animate__fadeIn">
      <h1 className="title text-center fw-bold ">{title}</h1>
      <hr className="line" />
    </div>
  );
};
export default Title;
